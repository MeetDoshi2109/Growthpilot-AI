# Copy to .env.local and fill in values
# For Vercel deployment set these in Vercel Dashboard > Project > Environment Variables

# Backend API URL (Railway/Render URL in production, localhost in dev)
NEXT_PUBLIC_API_URL=http://localhost:8000

# Sandbox mode indicator
NEXT_PUBLIC_SANDBOX=true
